query GetPost($id: Post.id) {
    post {
        @where { id == $id  }

        id
        title
        content
        published
        createdAt
        authorUserId
        users {
            id
            name
            email
        }
    }
}

