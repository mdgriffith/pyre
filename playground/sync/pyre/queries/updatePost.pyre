update UpdatePost($id: Post.id, $title: String, $content: String, $published: Bool) {
    post {
        @where { id == $id  }

        title = $title
        content = $content
        published = $published
    }
}

