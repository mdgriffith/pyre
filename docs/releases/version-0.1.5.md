# Pyre - 0.1.5

`0.1.5` strengthens generated server bindings. Rust server code now represents timestamps as either Unix seconds or text, matching Pyre's runtime encodings. TypeScript server session validators now preserve custom database types instead of accepting arbitrary values.
